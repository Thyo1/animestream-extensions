package com.thyo.animestream.network; import okhttp3.Interceptor; import okhttp3.Response; class CloudflareKiller : Interceptor { override fun intercept(chain: Interceptor.Chain): Response = chain.proceed(chain.request()) }
